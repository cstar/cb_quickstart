Chicago Boss sample app
=======================

Clone the project and do the rebar dance

```console
git archive --format=tar --remote=https://github.com/cstar/cb_quickstart.git master | tar -xv
make PROJECT=<your app name>

```
